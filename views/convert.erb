<h1>Convert <%= @first_currency %></h1>

<p>Go <a href="/">back.</a>

<ul>
  <% @list.keys.each do |currency_code| %>
    <li>
      <a href="/<%= @first_currency %>/<%= currency_code %>">
      Convert 1 <%= @first_currency %> to <%= currency_code %>...
      </a>
    </li>
  <% end %>
</ul>
